# Loaded by VitePress for the "Open dashboard" link on the docs home page.
VITE_APP_URL=https://armith.onrender.com

# Optional: override playground default backend (RestApiPlayground uses sandbox URL by default).
# VITE_BACKEND_URL=https://armith-backend-live.onrender.com
